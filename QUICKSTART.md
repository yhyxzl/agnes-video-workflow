# Agnes Studio -- 快速启动

## 双击 `env_start.bat`

全自动无人值守：环境检查 -> 装依赖 -> 清端口 -> 启动 -> 开浏览器。

无需按任何键，10 秒后启动器自动关闭。

## 手动命令 (备用)

```
cd frontend && npx vite --port 5173 --host          # 前端
cd backend && venv\Scripts\activate && python main.py  # 后端
```
